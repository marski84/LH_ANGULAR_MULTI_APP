import { SubmitButtonNamePipe } from './submit-button-name.pipe';

describe('SubmitButtonNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SubmitButtonNamePipe();
    expect(pipe).toBeTruthy();
  });
});
