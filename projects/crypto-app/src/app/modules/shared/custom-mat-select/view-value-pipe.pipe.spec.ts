import { ViewValuePipePipe } from './view-value-pipe.pipe';

describe('ViewValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ViewValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
